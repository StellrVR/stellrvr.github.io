---
layout: layout.njk
title: blog
description: thoughts, projects, and whatever else
permalink: /blog/
---
<div class="content-card">
    {% if collections.posts.length > 0 %}
    <div class="post-list">
        {% for post in collections.posts %}
        <article class="post-preview">
            <h2><a href="{{ post.url | url }}">{{ post.data.title }}</a></h2>
            <div class="post-meta">
                {{ post.date | readableDate }}
                {% if post.data.author %}
                <span style="margin: 0 0.5rem; color: var(--ctp-surface2);">//</span>
                {{ post.data.author }}
                {% endif %}
            </div>
            {% if post.data.excerpt %}
            <p class="post-excerpt">{{ post.data.excerpt }}</p>
            {% endif %}
            {% if post.data.tags %}
            <div class="post-tags">
                {% for tag in post.data.tags %}
                <span class="tag tag-accent">{{ tag }}</span>
                {% endfor %}
            </div>
            {% endif %}
        </article>
        {% endfor %}
    </div>
    {% else %}
    <div style="text-align: center; padding: 3rem; color: var(--ctp-overlay1);">
        <p style="font-family: 'Space Mono', monospace; font-size: 0.9rem;">no posts yet... check back soon!</p>
    </div>
    {% endif %}
</div>